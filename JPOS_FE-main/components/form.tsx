const MyForm = ({ children }: { children: React.ReactNode }) => {
    return (
        <div className="flex size-full flex-col flex-nowrap gap-9">
            <form />
        </div>
    );
};

export default MyForm;
